import MSignup from "@/components/MSignup";

/* Create account — onboarding screen 04. The screen itself is components/MSignup.tsx. */
export const dynamic = "force-dynamic";

export default function MSignupPage() {
  return <MSignup />;
}
